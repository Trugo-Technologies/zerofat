namespace ZeroFat.Shared.Constans;

public static class ClientPortalSettingConstants
{
    //int
    public const string OffsetSubscriptionInDays = nameof(OffsetSubscriptionInDays);

    // TimeOnly in UTC (HH:mm)
    public const string CutoffTime = nameof(CutoffTime);

    // Enum (BASED ON TIME, BASED ON CALORIES)
    public const string NutriPlanBasedStartegy = nameof(NutriPlanBasedStartegy);

    // int
    public const string WeeklyCaloricDeficit = nameof(WeeklyCaloricDeficit);

    // int
    public const string WeeklyCaloricSurplus = nameof(WeeklyCaloricSurplus);

    // in weeks
    public const string DefaultNutriPlanTimeAvailable = nameof(DefaultNutriPlanTimeAvailable);

    // int
    public const string MonthlyPackageSubsciptionDiscount = nameof(MonthlyPackageSubsciptionDiscount);

    // int
    public const string MinimumSubsciption = nameof(MinimumSubsciption);

    // int
    public const string ThreeMonthlyPackageSubsciptionDiscount = nameof(ThreeMonthlyPackageSubsciptionDiscount);

    public const string OnePointEqualInMoney = nameof(OnePointEqualInMoney);


    public const string EnableDailyMealSelections = nameof(EnableDailyMealSelections);

}
